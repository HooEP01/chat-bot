package faqtype

import (
	"github.com/HooEP01/chat-bot/views/layouts"
)

templ Hello(name string) {
	@layouts.Base() {
		<div class="flex flex-col items-center">
			<p>Hello, { name }</p>
		</div>
	}
}